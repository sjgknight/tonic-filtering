<%
  display_items = tonic_collection.select do |item|
    eval(my_filters)
  end
%>

<div id="collection-container" class="mt-4 grid grid-cols-1">
  <% display_items.each do |item| %>
    <%= partial 'templates/collection/item_include', locals: { item: item } %>
  <% end %>
</div>
