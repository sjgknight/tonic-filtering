<% renderer = Redcarpet::Render::HTML.new(with_toc_data: true) %>
<% markdown = Redcarpet::Markdown.new(renderer, footnotes: true) %>
<% html = markdown.render(markdown_text).html_safe %>

<%= html %>
